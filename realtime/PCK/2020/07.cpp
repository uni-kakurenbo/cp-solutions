#include <bits/stdc++.h>
using namespace std;

int main() {
    int w, d, h, c; cin >> w >> d >> h >> c;

    for(int i=0; i<c; ++i) {
        int x, y, z; cin >> x >> y >> z;
        t[x][y] -= z;
    }

    {
        vector<
    }
}
